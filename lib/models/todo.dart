// Define the Todo class

class Todo {
  // Necessary properties for a Todo object
  int id;
  String content;
  bool completed;

  // Constructor for the Todo class
  Todo({
    required this.id,
    required this.content,
    required this.completed,
  });
}

// Looks like C# class definition with the additional required keyword 
